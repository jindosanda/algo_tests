# Exercise 290 (m23). Write an algorithm (in python) square_root(n) that, given a non-negative integer n,
# returns the square root of n. square_root(n) may only use the basic arithmetic operations of addition, 
# subtraction, multiplication and division (integer), and must run in O(log n) time.


def square_root(n):
    pass

